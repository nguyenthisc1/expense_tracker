// TODO: Define TransactionEvent (sealed class)
// Events (past tense):
//   - TransactionStarted (initial load)
//   - TransactionAdded (carries new entity params)
//   - TransactionUpdated (carries updated entity)
//   - TransactionDeleted (carries id)
//   - TransactionFiltered (carries date range + category filters)
